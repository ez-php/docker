services:

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ez-php-{{MODULE_NAME}}-app
    volumes:
      - .:/var/www/html
    working_dir: /var/www/html
    env_file:
      - .env
