services:

  app:
    depends_on:
      redis:
        condition: service_started

  redis:
    image: redis:7-alpine
    container_name: ez-php-{{MODULE_NAME}}-redis
    ports:
      - "${REDIS_PORT:-6379}:6379"
